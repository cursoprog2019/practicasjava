package ejercicios2;

/**
 * Ejercicio7_v1
 * Programa que lea números enteros por teclado 
 * y para cada número introducido indique
 * si es positivo o negativo y si es par o impar.
 * Se deben realizar tres versiones del programa:
 * 1. En la primera versión se utilizará un bucle while. 
 *    La lectura de números finalizará cuando se introduzca un cero.
 */
public class Ejercicio7_v1 {

    public static void main(String[] args) {
        Scanner entrada = new Scanner(System.in);
        int numero ;
        // pedir número
        System.out.print("Dime número: ");
        numero = entrada.nextInt();
        // mientras el número sea distinto de 0
        while(numero != 0) {
            // ver si es positivo o negativo
            
            // ver si es par o impar

        }

    }
}